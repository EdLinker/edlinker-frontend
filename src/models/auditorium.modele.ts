export interface AuditoriumModele {
  auditoriumId: number;
  groupId: number;
  groupName: string;
  subjectName: string;
  courceNumber: number;
  studentsCount: number;
  groupLeaderName?: string;
}
